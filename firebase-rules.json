{
  "//": "LOKESHVERSE — Firebase Realtime Database security rules for the live visitors counter. Paste these into Firebase Console -> Realtime Database -> Rules.",
  "rules": {
    ".read": false,
    ".write": false,
    "liveVisitors": {
      ".read": true,
      "$sessionId": {
        ".write": true,
        ".validate": "newData.hasChildren(['lastSeen', 'path']) && newData.child('lastSeen').isNumber() && newData.child('path').isString() && newData.child('path').val().length < 200"
      }
    }
  }
}
